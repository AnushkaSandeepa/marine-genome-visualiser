install.packages("rsconnect")     
setwd("path/to/marine-genome-visualiser")

rsconnect::writeManifest(
  appDir = ".",
  verbose = TRUE
)

install.packages("jsonlite")
jsonlite::fromJSON("manifest.json")  
